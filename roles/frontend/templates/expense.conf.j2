http {
    proxy_http_version 1.1;

    server {
        location /api/ {
            proxy_pass http://{{ BACKEND_HOST }}:8080/;
        }

        location /health {
            stub_status on;
            access_log off;
        }
    }
}
